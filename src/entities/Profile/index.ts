import {type Profile, ValidateProfileError} from "./model/types/profile";
import {ProfileCard} from './ui/ProfileCard/ProfileCard';
import {filledProfile, emptyProfile} from "./model/mocks/profile";

export {
    Profile,
    ProfileCard,
    ValidateProfileError,
    filledProfile,
    emptyProfile
};